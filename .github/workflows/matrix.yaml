name: matrix hands on
on: push
jobs:
  node-version:
    strategy:
      matrix:
        node_version: [6, 8, 10]
        os: [macos-latest, ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Log node Version
        run: node -V
      - uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node_version }}
      - name: Log node version
        run: node -V
